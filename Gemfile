source "https://rubygems.org"

# GitHub Pages 공식 gem (Jekyll + 플러그인 버전 고정)
gem "github-pages", group: :jekyll_plugins

# 로컬 serve 용 (Ruby 3.x 이상일 때 필요)
gem "webrick", "~> 1.8"

# Faraday v2 경고 제거용
gem "faraday-retry"

# GitHub Pages에서 자동 관리하므로 따로 적을 필요 없는 gem은 삭제
# (jekyll, jekyll-feed, jekyll-sitemap, jekyll-redirect-from, jemoji 등은 github-pages 안에 포함됨)

# connection_pool 버전 고정도 보통 필요 없음 → 지우는 게 안정적
